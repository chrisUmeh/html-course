title: HTML
description: >-
  HTML is used to create a structure of a web page. It is the building block of
  a web page.
template:
  repo: html-template
  name: html
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
